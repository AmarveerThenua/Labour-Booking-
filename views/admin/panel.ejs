<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - Labor Booking</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .admin-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            margin: 20px;
            overflow: hidden;
        }
        
        .admin-header {
            background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
            color: white;
            padding: 40px;
            text-align: center;
        }
        
        .admin-content {
            padding: 40px;
        }
        
        .stats-card {
            background: white;
            border-radius: 15px;
            padding: 30px;
            text-align: center;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: none;
            margin-bottom: 20px;
        }
        
        .stats-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.15);
        }
        
        .stats-icon {
            font-size: 3rem;
            margin-bottom: 15px;
        }
        
        .stats-number {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 10px;
        }
        
        .stats-label {
            color: #6c757d;
            font-weight: 600;
        }
        
        .filter-section {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 30px;
        }
        
        .form-select {
            border: 2px solid #e9ecef;
            border-radius: 12px;
            padding: 12px 20px;
            font-size: 16px;
            transition: all 0.3s ease;
        }
        
        .form-select:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        
        .table-container {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }
        
        .table {
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        .table thead th {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 20px 15px;
            font-weight: 600;
        }
        
        .table tbody tr {
            transition: all 0.3s ease;
        }
        
        .table tbody tr:hover {
            background-color: #f8f9ff;
            transform: scale(1.01);
        }
        
        .table tbody td {
            padding: 15px;
            border: none;
            border-bottom: 1px solid #e9ecef;
        }
        
        .badge {
            border-radius: 20px;
            padding: 8px 16px;
            font-weight: 600;
        }
        
        .btn-admin {
            border-radius: 12px;
            padding: 10px 20px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
        }
        
        .btn-admin:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }
        
        .btn-view {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        
        .btn-delete {
            background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
            color: white;
        }
        
        .section-title {
            color: #333;
            font-weight: bold;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .alert {
            border-radius: 12px;
            border: none;
            padding: 15px 20px;
        }
        
        .navbar-brand {
            font-weight: bold;
            font-size: 1.5rem;
        }
        
        .nav-link {
            border-radius: 8px;
            transition: all 0.3s ease;
        }
        
        .nav-link:hover {
            background-color: rgba(255,255,255,0.1);
        }
        
        @media (max-width: 768px) {
            .admin-container {
                margin: 10px;
            }
            .admin-header, .admin-content {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark" style="background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);">
        <div class="container">
            <a class="navbar-brand" href="/admin/panel">
                <i class="bi bi-shield-check me-2"></i>Admin Panel
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/panel">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="admin-container">
        <!-- Admin Header -->
        <div class="admin-header">
            <h1 class="display-4 fw-bold mb-3">
                <i class="bi bi-shield-check me-3"></i>Admin Dashboard
            </h1>
            <p class="lead mb-0">Manage users, laborers, and bookings across the platform</p>
        </div>

        <!-- Admin Content -->
        <div class="admin-content">
            <!-- Flash Messages -->
            <% if (success && success.length > 0) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="bi bi-check-circle me-2"></i><%= success %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            <% } %>

            <% if (error && error.length > 0) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="bi bi-exclamation-triangle me-2"></i><%= error %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            <% } %>

            <!-- Dashboard Stats -->
            <div class="row mb-5">
                <div class="col-md-4">
                    <div class="stats-card">
                        <i class="bi bi-people stats-icon text-primary"></i>
                        <div class="stats-number text-primary">
                            <%= users.filter(u => u.role === 'user').length %>
                        </div>
                        <div class="stats-label">Total Users</div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="stats-card">
                        <i class="bi bi-tools stats-icon text-success"></i>
                        <div class="stats-number text-success">
                            <%= users.filter(u => u.role === 'labor').length %>
                        </div>
                        <div class="stats-label">Total Laborers</div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="stats-card">
                        <i class="bi bi-calendar-check stats-icon text-warning"></i>
                        <div class="stats-number text-warning">
                            <%= bookings.length %>
                        </div>
                        <div class="stats-label">Total Bookings</div>
                    </div>
                </div>
            </div>

            <!-- Filter Section -->
            <div class="filter-section">
                <form method="get" action="">
                    <div class="row align-items-center">
                        <div class="col-md-4">
                            <label for="role" class="form-label fw-bold">
                                <i class="bi bi-funnel me-2"></i>Filter by Role:
                            </label>
                        </div>
                        <div class="col-md-6">
                            <select name="role" id="role" onchange="this.form.submit()" class="form-select">
                                <option value="" <%= !selectedRole ? 'selected' : '' %>>All Users</option>
                                <option value="user" <%= selectedRole === 'user' ? 'selected' : '' %>>Users Only</option>
                                <option value="labor" <%= selectedRole === 'labor' ? 'selected' : '' %>>Laborers Only</option>
                            </select>
                        </div>
                        <div class="col-md-2">
                            <button type="submit" class="btn btn-primary btn-admin w-100">
                                <i class="bi bi-search me-1"></i>Filter
                            </button>
                        </div>
                    </div>
                </form>
            </div>

            <!-- Users Table -->
            <div class="table-container">
                <h3 class="section-title">
                    <i class="bi bi-people-fill text-primary"></i>All Users
                </h3>
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th><i class="bi bi-person me-2"></i>Name</th>
                                <th><i class="bi bi-envelope me-2"></i>Email</th>
                                <th><i class="bi bi-telephone me-2"></i>Mobile</th>
                                <th><i class="bi bi-shield me-2"></i>Role</th>
                                <th><i class="bi bi-gear me-2"></i>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(user => { %>
                                <tr>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <i class="bi bi-person-circle me-2" style="font-size: 1.5rem; color: #667eea;"></i>
                                            <strong><%= user.name %></strong>
                                        </div>
                                    </td>
                                    <td><%= user.email %></td>
                                    <td><%= user.mobile %></td>
                                    <td>
                                        <span class="badge <%= user.role === 'user' ? 'bg-info' : user.role === 'labor' ? 'bg-primary' : 'bg-secondary' %>">
                                            <i class="bi <%= user.role === 'user' ? 'bi-person' : user.role === 'labor' ? 'bi-tools' : 'bi-shield-check' %> me-1"></i>
                                            <%= user.role %>
                                        </span>
                                    </td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <a href="/admin/user/<%= user._id %>" class="btn btn-sm btn-view btn-admin me-2">
                                                <i class="bi bi-eye me-1"></i>View
                                            </a>
                                            <form method="POST" action="/admin/user/delete/<%= user._id %>?_method=DELETE" 
                                                  style="display:inline-block;"
                                                  onsubmit="return confirm('Are you sure you want to delete <%= user.name %>? This action cannot be undone.');">
                                                <button class="btn btn-sm btn-delete btn-admin">
                                                    <i class="bi bi-trash me-1"></i>Delete
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Bookings Table -->
            <div class="table-container">
                <h3 class="section-title">
                    <i class="bi bi-calendar-check text-success"></i>All Bookings
                </h3>
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th><i class="bi bi-person me-2"></i>User</th>
                                <th><i class="bi bi-tools me-2"></i>Laborer</th>
                                <th><i class="bi bi-flag me-2"></i>Status</th>
                                <th><i class="bi bi-calendar me-2"></i>Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% bookings.forEach(b => { %>
                                <tr>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <i class="bi bi-person-circle me-2" style="font-size: 1.2rem; color: #667eea;"></i>
                                            <strong><%= b.user?.name || 'N/A' %></strong>
                                        </div>
                                    </td>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <i class="bi bi-tools me-2" style="font-size: 1.2rem; color: #28a745;"></i>
                                            <strong><%= b.labor?.user?.name || 'N/A' %></strong>
                                        </div>
                                    </td>
                                    <td>
                                        <span class="badge <%= b.status === 'Pending' ? 'bg-warning' : b.status === 'Accepted' ? 'bg-success' : 'bg-danger' %>">
                                            <i class="bi <%= b.status === 'Pending' ? 'bi-clock' : b.status === 'Accepted' ? 'bi-check-circle' : 'bi-x-circle' %> me-1"></i>
                                            <%= b.status %>
                                        </span>
                                    </td>
                                    <td>
                                        <i class="bi bi-calendar-event me-2 text-muted"></i>
                                        <%= b.date.toLocaleDateString() %>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>