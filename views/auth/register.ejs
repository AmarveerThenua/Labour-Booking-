<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Labor Booking</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px 0;
        }
        
        .register-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
            max-width: 1000px;
            width: 100%;
        }
        
        .register-sidebar {
            background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
            color: white;
            padding: 60px 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }
        
        .register-form {
            padding: 60px 40px;
        }
        
        .form-control {
            border: 2px solid #e9ecef;
            border-radius: 12px;
            padding: 15px 20px;
            font-size: 16px;
            transition: all 0.3s ease;
        }
        
        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        
        .form-select {
            border: 2px solid #e9ecef;
            border-radius: 12px;
            padding: 15px 20px;
            font-size: 16px;
            transition: all 0.3s ease;
        }
        
        .form-select:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        
        .btn-register {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 12px;
            padding: 15px 30px;
            font-weight: 600;
            font-size: 16px;
            transition: all 0.3s ease;
            width: 100%;
        }
        
        .btn-register:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.3);
        }
        
        .form-label {
            font-weight: 600;
            color: #495057;
            margin-bottom: 8px;
        }
        
        .alert {
            border-radius: 12px;
            border: none;
        }
        
        .feature-icon {
            font-size: 3rem;
            margin-bottom: 20px;
        }
        
        .back-home {
            color: white;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 30px;
            transition: all 0.3s ease;
        }
        
        .back-home:hover {
            color: #f8f9fa;
            transform: translateX(-5px);
        }
        
        .role-card {
            border: 2px solid #e9ecef;
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-bottom: 15px;
        }
        
        .role-card:hover {
            border-color: #667eea;
            background-color: #f8f9ff;
        }
        
        .role-card.selected {
            border-color: #667eea;
            background: linear-gradient(135deg, #f8f9ff 0%, #e8f0ff 100%);
        }
        
        .role-icon {
            font-size: 2rem;
            color: #667eea;
            margin-bottom: 10px;
        }
        
        @media (max-width: 768px) {
            .register-sidebar {
                padding: 40px 20px;
            }
            .register-form {
                padding: 40px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="register-container">
        <div class="row g-0">
            <!-- Sidebar -->
            <div class="col-lg-5">
                <div class="register-sidebar">
                    <a href="/" class="back-home">
                        <i class="bi bi-arrow-left"></i>
                        Back to Home
                    </a>
                    
                    <i class="bi bi-person-plus feature-icon"></i>
                    <h2 class="mb-4">Join Our Community!</h2>
                    <p class="lead mb-4">Create your account and start connecting with skilled laborers or find work opportunities.</p>
                    
                    <div class="row text-center">
                        <div class="col-6">
                            <i class="bi bi-shield-check mb-2" style="font-size: 1.5rem;"></i>
                            <p class="small">Secure Registration</p>
                        </div>
                        <div class="col-6">
                            <i class="bi bi-people mb-2" style="font-size: 1.5rem;"></i>
                            <p class="small">Trusted Community</p>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Register Form -->
            <div class="col-lg-7">
                <div class="register-form">
                    <div class="text-center mb-4">
                        <h3 class="fw-bold text-dark">Create Account</h3>
                        <p class="text-muted">Fill in your details to get started</p>
                    </div>
                    
                    <!-- Flash Messages -->
                    <% if (success && success.length > 0) { %>
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <%= success %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    <% } %>
                    
                    <% if (error && error.length > 0) { %>
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <%= error %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    <% } %>
                    
                    <form action="/register" method="POST">
                        <div class="row">
                            <div class="col-md-6 mb-4">
                                <label for="name" class="form-label">
                                    <i class="bi bi-person me-2"></i>Full Name
                                </label>
                                <input type="text" class="form-control" id="name" name="name" required 
                                       placeholder="Enter your full name">
                            </div>
                            
                            <div class="col-md-6 mb-4">
                                <label for="email" class="form-label">
                                    <i class="bi bi-envelope me-2"></i>Email Address
                                </label>
                                <input type="email" class="form-control" id="email" name="email" required 
                                       placeholder="Enter your email">
                            </div>
                        </div>
                        
                        <div class="row">
                            <div class="col-md-6 mb-4">
                                <label for="mobile" class="form-label">
                                    <i class="bi bi-telephone me-2"></i>Mobile Number
                                </label>
                                <input type="text" class="form-control" id="mobile" name="mobile" required 
                                       placeholder="Enter your mobile number">
                            </div>
                            
                            <div class="col-md-6 mb-4">
                                <label for="password" class="form-label">
                                    <i class="bi bi-lock me-2"></i>Password
                                </label>
                                <input type="password" class="form-control" id="password" name="password" required 
                                       placeholder="Create a password">
                            </div>
                        </div>
                        
                        <div class="mb-4">
                            <label class="form-label">
                                <i class="bi bi-briefcase me-2"></i>Register As
                            </label>
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="role-card" onclick="selectRole('user')" id="user-card">
                                        <i class="bi bi-person-circle role-icon"></i>
                                        <h6 class="mb-2">User</h6>
                                        <p class="small text-muted mb-0">I want to hire laborers</p>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="role-card" onclick="selectRole('labor')" id="labor-card">
                                        <i class="bi bi-tools role-icon"></i>
                                        <h6 class="mb-2">Laborer</h6>
                                        <p class="small text-muted mb-0">I want to offer my services</p>
                                    </div>
                                </div>
                            </div>
                            <input type="hidden" name="role" id="role-input" value="user" required>
                        </div>
                        
                        <button type="submit" class="btn btn-primary btn-register mb-4">
                            <i class="bi bi-person-plus me-2"></i>Create Account
                        </button>
                    </form>
                    
                    <div class="text-center">
                        <p class="text-muted mb-0">
                            Already have an account? 
                            <a href="/login" class="text-decoration-none fw-bold" style="color: #667eea;">
                                Sign In
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        function selectRole(role) {
            // Remove selected class from all cards
            document.querySelectorAll('.role-card').forEach(card => {
                card.classList.remove('selected');
            });
            
            // Add selected class to clicked card
            if (role === 'user') {
                document.getElementById('user-card').classList.add('selected');
            } else {
                document.getElementById('labor-card').classList.add('selected');
            }
            
            // Update hidden input
            document.getElementById('role-input').value = role;
        }
        
        // Set default selection
        document.addEventListener('DOMContentLoaded', function() {
            selectRole('user');
        });
    </script>
</body>
</html>
